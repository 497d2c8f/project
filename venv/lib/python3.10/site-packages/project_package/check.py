def check_data_type(data, type_):
		if not isinstance(data, type_):
			raise TypeError(f'тип данных {type(data)}, должен быть {type_}')


def check_bytes_length(data, length):
		if len(data) != length:
			raise ValueError(f'количество байтов {len(data)}, должно быть {length} Б')
